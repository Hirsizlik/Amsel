# Amsel

A MTG Arena Card Collection Manager.

The card collection is directly read from memory of a running MTG Arena client.
The data is then provided as Web application using Blazor.

Once loaded, the data will be cached so that offline use is possible.
